---
name: Bug report
about: Something in the pipeline, statistics, or docs is wrong or breaks
title: "[bug] "
labels: bug
---

**What happened**
A clear description of the bug.

**To reproduce**
Steps or a minimal snippet. If it's a pipeline stage, include the command and
the config (or the relevant fields):

```bash
python run.py <stage> --config config/<file>.yaml
```

**Expected behaviour**
What you expected instead.

**Environment**
- OS / GPU (or CPU-only):
- Python version:
- `foregrounds_diffusion` version or git commit:
- Key deps if relevant (`torch`, `healpy`, `numpy`):

**Logs / traceback**
```
paste here
```
